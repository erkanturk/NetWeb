@{
    ViewData["title"] = "Masa" + ViewBag.MasaNumarasi + " - Rezervasyon Detayları";
}
<h2 class="mb-4">Masa @ViewBag.MasaNumarasi - Rezervasyon Detayları</h2>
<div class="mb-3">
    <a asp-action="Ekle" asp-controller="Rezervasyon" asp-route-masaId="@ViewBag.MasaId" class="btn btn-success">Yeni Rezervasyon</a>
    <a asp-action="Index" asp-controller="Garson" class="btn btn-secondary">Masaya Geri Dön</a>
</div>
@if (Model.Count == 0)
{
    <div class="alert alert-info">Bu masaya ait gelecekte bir rezervasyon bulunamadı</div>
}
else
{
    <table class="table table-striped">
        <thead>
            <tr>
                <th>Müşteri Adı</th>
                <th>Rezervasyon Tarihi</th>
                <th>Oluşturma Tarihi</th>
                <th>İşlemler</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var rezervasyon in Model)
            {
                <tr>
                    <td>@rezervasyon.MusteriAdi</td>
                    <td>@rezervasyon.RezervasyonTarihi.ToString("dd-MM-yyyy HH:mm:ss")</td>
                    <td>@rezervasyon.OlusturmaTarihi.ToString("dd-MM-yyyy HH:mm:ss")</td>
                    <td>
                        <form asp-action="RezervasyonAktifEt" asp-controller="Garson" method="post" style="display:inline;">
                            <input type="hidden" name="rezervasyonId" value="@rezervasyon.Id" />
                            <button type="submit" class="btn btn-secondary btn-sm">Aktif Et</button>
                        </form>
                        <a asp-controller="Rezervasyon" asp-action="Duzenle" asp-route-id="@rezervasyon.Id" class="btn btn-warning btn-sm me-2">Düzenle</a>
                        <a asp-controller="Rezervasyon" asp-action="Sil" asp-route-id="@rezervasyon.Id" class="btn btn-warning btn-sm me-2">Sil</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
}